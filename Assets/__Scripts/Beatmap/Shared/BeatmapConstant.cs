public static class BeatmapConstant
{
    public const float LaneSize = 0.6f;
    // Keep scaled event-node models just above the grid surface instead of centering them in a full-height lane.
    public const float EventNodeGroundedCenterY = 0.4f;
    public const float ObstacleYOffset = -0.15f;
    public const float YOffset = 0.25f;
    public const float ZOffset = 1f;

    public const float PlayerYOffset = 0.6f;
}
